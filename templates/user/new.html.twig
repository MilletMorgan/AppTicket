{% extends 'base.html.twig' %}

{% block body %}
    <h1>Inscription</h1>
    <br>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}